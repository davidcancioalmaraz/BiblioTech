# BiblioTech

A library management system for books and loans, built as a static site with **HTML and CSS only** — no JavaScript, no frameworks, no backend.

Six pages: login, dashboard, book list and form, loan list and form.

Open `index.html` in any browser. Nothing to install, nothing to build.
